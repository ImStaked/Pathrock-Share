# Prometheus Alert Manager Config
# See https://prometheus.io/docs/alerting/configuration/ for documentation.

global:
  # The smarthost and SMTP sender used for mail notifications.
  smtp_smarthost: 'mail.your-server.de:465'
  smtp_from: 'notify@imstaked.com'
  smtp_auth_username: 'notify@imstaked.com'
  smtp_auth_password: ''

# The directory from which notification templates are read.
templates: 
- '/etc/prometheus/alertmanager_templates/*.tmpl'

# The root route on which each incoming alert enters.
route:
  # The labels by which incoming alerts are grouped together. For example,
  # multiple alerts coming in for cluster=A and alertname=LatencyHigh would
  # be batched into a single group.
  group_by: ['alertname', 'cluster', 'service']

  # When a new group of alerts is created by an incoming alert, wait at
  # least 'group_wait' to send the initial notification.
  # This way ensures that you get multiple alerts for the same group that start
  # firing shortly after another are batched together on the first 
  # notification.
  group_wait: 30s

  # When the first notification was sent, wait 'group_interval' to send a batch
  # of new alerts that started firing for that group.
  group_interval: 5m

  # If an alert has successfully been sent, wait 'repeat_interval' to
  # resend them.
  repeat_interval: 3h 

  # A default receiver
  receiver: imstaked-email

  # All the above attributes are inherited by all child routes and can 
  # overwritten on each.

  # The child route trees.
  routes:
  # This routes performs a regular expression match on alert labels to
  # catch alerts that are related to a list of services.
  - match_re:
      service: ^(haproxy|grafana|.*exporter*.)$
    receiver: imstaked-email
    # The service has a sub-route for critical alerts, any alerts
    # that do not match, i.e. severity != critical, fall-back to the
    # parent node and are sent to 'imstaked-email'
    routes:
    - match:
        severity: critical
      receiver: imstaked-email
  - match:
      service: files
    receiver: pathrock-email

    routes:
    - match:
        severity: critical
      receiver: pathrock-email

  # This route handles all alerts coming from a database service. If there's
  # no team to handle it, it defaults to the DB team.
  - match:
      service: database
    receiver: database-email
    # Also group alerts by affected database.
    group_by: [alertname, cluster, database]
    routes:
    - match:
        owner: imstaked
      receiver: imstaked-email
    - match:
        owner: pathrocknetwork
      receiver: pathrock-email


# Inhibition rules allow to mute a set of alerts given that another alert is
# firing.
# We use this to mute any warning-level notifications if the same alert is 
# already critical.
inhibit_rules:
- source_match:
    severity: 'critical'
  target_match:
    severity: 'warning'
  # Apply inhibition if the alertname is the same.
  equal: ['alertname', 'cluster', 'service']


receivers:
- name: 'imstaked-email'
  email_configs:
  - to: 'notify@imstaked.com'

- name: 'pathrock-email'
  email_configs:
  - to: 'pathrock@pathrocknetwork.org'

- name: 'database-email'
  email_configs:
  - to: 'notify@imstaked.com'

#- name: 'imstaked-discord'
#  discord_configs:
#  send_resolved: true 
#  webhook_url: https://
#  title: '{{ template "discord.default.title" . }}' 
#  message: <tmpl_string> | default = '{{ template "discord.default.message" . }}' 
#  http_config: global.http_config

#- name: 'imstaked-telegram'
#  send_resolved: true 
#  api_url: global.telegram_api_url 
#  bot_token: <secret> 
#  bot_token_file: <filepath> 
#  chat_id: <int> 
#  message: '{{ template "telegram.default.message" .}}' 
#  disable_notifications: false 
#  parse_mode: "HTML" 
#  http_config: <http_config> | default = global.http_config 

#- name: 'pathrock-discord'
#  discord_configs:
#  send_resolved: true 
#  webhook_url: https://
#  title: '{{ template "discord.default.title" . }}' 
#  message: <tmpl_string> | default = '{{ template "discord.default.message" . }}' 
#  http_config: global.http_config

#- name: 'pathrock-telegram'
#  send_resolved: true 
#  api_url: global.telegram_api_url 
#  bot_token: <secret> 
#  bot_token_file: <filepath> 
#  chat_id: <int> 
#  message: '{{ template "telegram.default.message" .}}' 
#  disable_notifications: false 
#  parse_mode: "HTML" 
#  http_config: <http_config> | default = global.http_config 
