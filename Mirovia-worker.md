# Node Setup

### Ubuntu
sudo apt update && sudo apt upgrade -y
### Python
sudo apt install python3.11 python3.11-venv gcc protobuf-compiler
### Rust
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
